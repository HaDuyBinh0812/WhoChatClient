import EmptyState from "@/components/emty-state";

function Chat() {
    return (
        <div className="hidden lg:block h-svh">
            <EmptyState />
        </div>
    );
}

export default Chat;
